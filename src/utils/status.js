import { ref } from 'vue';

export const isLoading = ref(false);
export const errorMessage = ref(null);
export const successMessage = ref(null);
export const isEditMode = ref(false);
export const showModal = ref(false);